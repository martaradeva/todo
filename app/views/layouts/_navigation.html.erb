<%# navigation styled for Zurb Foundation 5 %>
<nav data-topbar>
  <div class="navbar-inverse">
    <div class="navbar-inner">
      <ul class="nav nav-tabs">
        <li ><%= link_to (I18n.t ('application.name')), root_path %></li>
        <li class="<%= 'active' if params[:action] == 'index'%>">
          <%= link_to (I18n.t ('nav.home')), root_path %>
        </li>
        <li class="<%= 'active' if params[:action] == 'new'%>">
          <%= link_to (I18n.t ('tasks.new.title')), new_task_path %>
        </li>
        <li class="<%= 'active' if params[:controller] == 'pages'%>">
          <%= link_to (I18n.t ('pages.about.title')), '../pages/about' %>
        </li>
      </ul>
    </div>
  </div>

  <div class="top-bar-section">
    <ul>
      <%= render 'layouts/navigation_links' %>
    </ul>
  </div>
</nav>
